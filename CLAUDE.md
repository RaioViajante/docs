# CLAUDE.md

Read [`AGENTS.md`](AGENTS.md) first — it is the primary instruction file for this repository (project purpose, stack, Git workflow, and working principles). This file only adds Claude-specific notes.

- Follow the commit rules in `AGENTS.md` exactly: Conventional Commits, one coherent change per commit, no push without explicit permission, no history rewrites.
- Keep implementations simple. Avoid unnecessary abstractions.
- Preserve the established RaioViajante visual identity (`docs/design.md`) instead of defaulting to generic documentation-site patterns.
- Never invent technical facts about projects (hum, sweep, orbit, etc.). If documentation requires a factual detail you don't have, ask the user or inspect the relevant source repository first.
- Don't duplicate content already covered in `AGENTS.md` — extend it there if a new agent-wide rule is needed, rather than restating it here.
