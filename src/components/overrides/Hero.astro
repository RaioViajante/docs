---
// Intentionally empty: this site never uses Starlight's marketing-style hero. The
// homepage sets frontmatter.hero only to skip the default PageTitle, and renders its
// own content through the StarlightPage slot instead.
---
