---
// Intentionally empty. Starlight renders <ThemeSelect/> inside the header and the
// mobile menu footer — both conditionally hidden by responsive/popover state, which
// would hide our always-visible fixed toggle along with them. The real toggle
// (ThemeToggle.astro) is rendered once, unconditionally, by the PageFrame override.
---
